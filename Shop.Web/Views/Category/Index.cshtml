@model List<Category>


<div class="container">
    <div class="row p-4">
       <div class="col-6">
           <h2 class="text-primary">Kategoriák</h2>
       </div>
        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Add" class="btn btn-danger">
                <i class="bi bi-plus-circle mr-1"> Kategoria hozzáadása</i>
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <theade>
            <tr>
                <th>Kategoria név</th>
                <th>Megjelenési sorrend</th>
            </tr>
        </theade>
        <tbody>
            @foreach (var obj in Model.OrderBy(x => x.DisplayOrder))
            {
                <tr>
                    <td>@obj.Name</td>
                    <td>@obj.DisplayOrder</td>
                </tr>
            }

        </tbody>
    </table>

</div>
